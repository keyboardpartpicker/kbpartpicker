services:
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./static:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api

  api:
    build: ./api
    ports:
      - "3001:3001"
    environment:
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=kbadmin
      - DB_PASSWORD=3StanPoopsockJuice
      - DB_NAME=kbdb
    depends_on:
      - postgres

  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_DB: kbdb
      POSTGRES_USER: kbadmin
      POSTGRES_PASSWORD: 3StanPoopsockJuice
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: